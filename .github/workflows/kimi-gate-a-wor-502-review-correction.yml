name: Kimi Gate A - WOR-502 Pre-PR Correction

on:
  push:
    branches:
      - rbj--637-kimi-gate-a-runner
    paths:
      - .github/workflows/kimi-gate-a-wor-502-review-correction.yml

permissions:
  contents: write

concurrency:
  group: kimi-gate-a-wor-502-pre-pr
  cancel-in-progress: false

jobs:
  correct:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      KIMI_API_KEY: ${{ secrets.KIMI_API_KEY }}
      MOONSHOT_BASE_URL: https://api.moonshot.ai/v1
      FEATURE_BRANCH: rbj--502-kimi-global-search
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare trusted correction broker and checkout Kimi feature
        id: runtime
        shell: bash
        run: |
          set -euo pipefail
          test -n "${KIMI_API_KEY:-}" || { echo 'KIMI_API_KEY missing'; exit 10; }
          cp tools/ai/kimi-gate-a-fix-wor502.py /tmp/kimi-gate-a-fix-wor502.py
          response="$(curl --fail --silent --show-error -H "Authorization: Bearer ${KIMI_API_KEY}" "${MOONSHOT_BASE_URL}/models")"
          model="$(printf '%s' "$response" | jq -r '([.data[].id | select(. == "kimi-k2.6")] + [.data[].id | select(test("^kimi-"; "i"))] + [.data[].id]) | unique | .[0] // empty')"
          test -n "$model" || exit 11
          echo "model=$model" >> "$GITHUB_OUTPUT"
          git fetch origin main "$FEATURE_BRANCH"
          git switch --force-create "$FEATURE_BRANCH" "origin/$FEATURE_BRANCH"
          git config user.name 'Kimi Worker'
          git config user.email 'kimi-worker@users.noreply.github.com'

      - name: Give pre-PR review findings back to Kimi
        env:
          KIMI_MODEL: ${{ steps.runtime.outputs.model }}
        shell: bash
        run: |
          cat >/tmp/wor502-review.txt <<'EOF'
          Pre-PR reviewer feedback. Items 1-8 may already be fixed; preserve those fixes. Correct the remaining race without redesigning the feature:

          1. Customer search must stay bounded: use exact generated params `{ query, limit: 5 }` consistently in query key and query function.
          2. Jobs React Query key must include presentation/user scope inputs (`canViewAllJobs`, `currentUserId`) that affect visible job results. This is cache scoping, not authorization.
          3. Use generated `JobStatus.InReview` / `JobStatus.Approved`, not duplicated string literals.
          4. Placeholder must accurately mention side, sag and kunde.
          5. Mixed results must be visibly distinguishable as Navigation / Sag / Kunde using existing layout/CSS.
          6. Do not claim stale previous results are being shown unless the query is deliberately configured for that. Keep source-specific error state and healthy-source results.
          7. Debounce remote requests using the existing `useDebounce<T>(value, delayMs)` around 180–200 ms while local command filtering remains immediate.
          8. Navigation must also be explicit in visible secondary copy.
          9. Current debounce implementation passes `debouncedQuery` from QuickNavigator into the hook, so after the raw input changes the previous remote jobs/customers can remain visible and selectable for ~200 ms. That is not acceptable: a fast click/Enter could open a result for the previous query. Keep the raw input query as the source of UI truth and ensure remote results/errors are hidden while debounce is pending. Prefer moving debounce responsibility into `useQuickNavigatorSearch` (or an equivalent clean design) so it can compare raw query vs debounced query. During that pending interval return no old remote jobs/customers, suppress old remote errors, and expose loading/pending state if the raw query has remote-search intent. Once debounced query catches up, query by the debounced value. Local command results must remain immediate.

          Keep the existing modular split. Do not add folders, Users/Docs remote search, new dependencies, CSS rewrite, or unrelated cleanup. This remains browser-unvalidated and draft-only.
          EOF
          python3 /tmp/kimi-gate-a-fix-wor502.py /tmp/wor502-review.txt

      - name: Enforce correction scope
        shell: bash
        run: |
          set -euo pipefail
          base="$(git merge-base origin/main HEAD)"
          { git diff --name-only "$base"; git ls-files --others --exclude-standard; } | sort -u | tee /tmp/feature-files.txt
          while IFS= read -r p; do
            case "$p" in
              src/FE/src/components/common/QuickNavigator.tsx|src/FE/src/components/common/QuickNavigatorResults.tsx|src/FE/src/components/common/quickNavigatorSearch.ts|src/FE/src/components/common/quickNavigatorSearch.test.ts|src/FE/src/components/common/quickNavigatorTypes.ts|src/FE/src/components/common/useQuickNavigatorSearch.ts) ;;
              *) echo "Out-of-scope feature path: $p"; exit 20 ;;
            esac
          done < /tmp/feature-files.txt

      - name: Validate corrected feature
        working-directory: src/FE
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          mapfile -t tests < <(find src/components/common -maxdepth 1 -type f -iname '*navigator*.test.ts*' -print)
          npx vitest run "${tests[@]}"
          mapfile -t files < <({ git diff --name-only "$(git merge-base origin/main HEAD)" -- '*.ts' '*.tsx'; git ls-files --others --exclude-standard -- 'src/components/common/*.ts' 'src/components/common/*.tsx'; } | sort -u | sed 's#^src/FE/##')
          npx eslint "${files[@]}"
          npm run build

      - name: Commit Kimi review correction
        shell: bash
        run: |
          set -euo pipefail
          git add src/FE/src/components/common
          git diff --cached --quiet && { echo 'Kimi made no review correction'; exit 30; }
          git commit -m 'RBJ-502: prevent stale results during search debounce'
          git push origin "$FEATURE_BRANCH"
          echo "Corrected Kimi head: $(git rev-parse HEAD)"
